//#include "RT_Log.hpp"

// Imlementation found in the header file

/*
template <typename T>
void RT_ log::log(RT_ log::LogLevel level, T msg)
{
	printf(color);
	printf(msg);
	printf(reset);
}

template <typename T, typename... Args>
void RT_ log::log(RT_ log::LogLevel level, T msg, Args... msgs)
{
	const char* color = colors[(int)level];
	printf(color);
	printf(msg);
	log(level, msgs);
	printf(reset);
}*/